[package]
name = "mock_prover"
version = "0.1.0"
authors = ["Calimero <info@calimero.network>"]
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-sdk = "4.0.0"
utils = { path = "../utils" }
types = { path = "../types" }
